export type CalculationSchema = {
  createdAt: string;
  email: string;
  firstParamether: number;
  id: string;
  img: string;
  name: string;
  operator: string;
  result: number;
  secondParamether: number;
};
